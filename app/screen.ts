import { Theater } from './theater';
//it is a class same as Screen.java inorder access the fields we need it
//it should have same fields as screen.java
export class Screen{

    screenId : number;
    screenName : string;
    rows : number;
    columns : number;
    theater : Theater;
  
}